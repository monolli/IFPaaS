language: python
python:
    - "3.5"
    - "3.6"
    - "3.7"
    - "3.8"
# Command to install dependencies
install:
    - pip install -r requirements.txt --upgrade pip
# Command to run tests
script:
    - python -m flake8
    - python -m pytest
branches:
    only:
        - master
